# Required on Vercel (Project Settings -> Environment Variables), then redeploy.
# Use your Render backend URL without /api — the app adds it automatically.
VITE_API_URL=https://your-backend.onrender.com

# Optional: override local dev proxy target (defaults to http://localhost:5000)
# VITE_BACKEND_PROXY_TARGET=http://localhost:5000
